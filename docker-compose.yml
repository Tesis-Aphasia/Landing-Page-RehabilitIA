version: '3.8'

services:
  rehabilitia-landing:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rehabilitia-landing
    ports:
      - "80:80"
    restart: unless-stopped
    # Optional: Add environment variables if needed
    # environment:
    #   - NODE_ENV=production
